# Config Schemas

This directory is reserved for schemas or documented validation contracts
for files in `rov_config/`. A later owner decision should choose whether
the MVP uses formal JSON Schema files, Python validation helpers, or both.
