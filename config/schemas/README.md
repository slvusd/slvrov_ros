# Config Schemas

This directory is reserved for JSON schemas or documented validation
contracts. Task 1 only creates the location; Task 2 should decide whether
the MVP uses formal JSON Schema files, Python validation helpers, or both.

