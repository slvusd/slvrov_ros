# Data Logging Placeholders

Future CSV or sensor data logging helpers should live here. Sensor work is
deferred for the MVP, but this location keeps future science data separate
from camera media files and web UI route code.

