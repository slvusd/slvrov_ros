# ROS Adapter Interfaces

Future route tests can use fake adapters from this package while the
project owner writes the matching ROS2 bridge or monitor nodes.

Adapters should keep Flask code easy to test without a running ROS graph.

