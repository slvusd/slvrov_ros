# Static Frontend Files

The MVP frontend uses plain HTML, CSS, and JavaScript. Shared styling and
browser utilities should live here, with mode-specific files added by
later tasks when each mode is implemented.
