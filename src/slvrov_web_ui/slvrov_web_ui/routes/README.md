# Flask Routes

Future Flask route modules should live here. Route handlers should focus
on HTTP request/response shape and call small adapter objects for ROS2 or
filesystem work, rather than putting ROS2 node logic directly in routes.

