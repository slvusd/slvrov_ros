# Prototype Pages

Put disposable static prototypes in this directory. Each prototype should be
self-contained so it can be opened directly from the demo shell index.

The `demo_template/` page is a starter only. It is not a proposed production
screen.
