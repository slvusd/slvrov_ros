# Prototype Pages

Put disposable static prototypes in this directory. Each prototype should be
self-contained so it can be opened directly from the demo shell index.

The active prototype is `apple_macos_direction/`. Older starters and legacy
concepts live under `ui_static/archive/` and `ui_archive/legacy_ui_examples/`.
